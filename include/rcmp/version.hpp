#pragma once

#include <cstddef>

namespace rcmp::version {

constexpr std::size_t major = RCMP_VERSION_MAJOR;
constexpr std::size_t minor = RCMP_VERSION_MINOR;
constexpr std::size_t patch = RCMP_VERSION_PATCH;

} // namespace rcmp
